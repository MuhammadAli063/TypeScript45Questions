// Guest List 
let guest_Lists: string[] = ['Ali', 'Umair', 'Hassan', 'Waqar'];

// for loop for each guest message
for (let j = 0; j < guest_Lists.length; j++) {
    console.log(`Hey, My Guest ${guest_Lists[j]}! Are you free tonight for dinner? Join me at 9 PM for dinner!`);
}
